version: '3.8'

services:
  jupyter:
    build: .
    ports:
      - "8888:8888"
    volumes:
      - .:/experiments
      - ../input_files:/input_files
    env_file:
      # Load environment variables (like OPENAI_API_KEY) from main directory
      - ../.env
